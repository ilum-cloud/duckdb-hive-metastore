# name: test/sql/hive_metastore.test
# description: test hive_metastore extension
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT hive_metastore('Sam');
----
Catalog Error: Scalar Function with name hive_metastore does not exist!

# Require statement will ensure this test is run with this extension loaded
require hive_metastore

# Confirm the extension works
query I
SELECT hive_metastore('Sam');
----
HiveMetastore Sam 🐥

query I
SELECT hive_metastore_openssl_version('Michael') ILIKE 'HiveMetastore Michael, my linked OpenSSL version is OpenSSL%';
----
true
